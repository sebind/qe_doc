# qe_doc

Static LASER documentation site in plain HTML/CSS, prepared as a fresh GitHub Pages project.

## What changed
- Each left-panel topic is now a separate standalone HTML page.
- The sidebar on every page links across all topic pages.
- Embedded GitBook iframes were removed so pages are published as native site content.

## Run locally
Open `index.html` in a browser.
